name: Build and Push Event Registration API

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build \
            -t kaveengayanga12/eventslk-event-registration-api:${{ github.sha }} \
            -t kaveengayanga12/eventslk-event-registration-api:latest \
            .

      - name: Push Docker image (SHA tag)
        run: docker push kaveengayanga12/eventslk-event-registration-api:${{ github.sha }}

      - name: Push Docker image (latest tag)
        run: docker push kaveengayanga12/eventslk-event-registration-api:latest

      - name: Update Kubernetes manifests (GitOps)
        run: |
          rm -rf eventslk-k8s-manifests
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/eventslk-org/eventslk-k8s-manifests.git
          cd eventslk-k8s-manifests

          sed -i "s|image: kaveengayanga12/eventslk-event-registration-api:.*|image: kaveengayanga12/eventslk-event-registration-api:${{ github.sha }}|g" event-api-deployment.yaml

          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          git add event-api-deployment.yaml
          git commit -m "ci: update event-registration-api image to ${{ github.sha }}" || echo "No changes to commit"

          git pull --rebase origin main
          git push origin main
